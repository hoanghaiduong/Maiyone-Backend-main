export enum FileTypes {
  IMAGE = 'IMAGE',
  EXCEL = 'EXCEL',
  OTHER = 'OTHER',
}

export enum ImagePath {
  CARD_PROVIDER = 'providers',
  CARD_SERVICE = 'services',
  CARD_USER = 'users',
  CARD_POST = 'posts',
}
